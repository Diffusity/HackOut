# Optional. Without it the app runs on the bundled JSON seed.
# Supabase: Project Settings > Database > Connection pooling (Transaction mode).
# Choose the Mumbai / ap-south-1 region for RBI data localisation.
DATABASE_URL=

# Optional. Without it, narration falls back to grounded templates.
GEMINI_API_KEY=
